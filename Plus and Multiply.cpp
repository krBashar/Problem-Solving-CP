#include<bits/stdc++.h>

using namespace std;

int main()
{

    int n,a,b,c,res,sum,count;
    vector <int> vec;
    cin>>n>>a>>b>>c;

    if(n%a==0)
        vec.push_back(n/a);
    else
    {
        if()
    }
    if(n%b==0)
        vec.push_back(n/b);
    if(n%c==0)
        vec.push_back(n/c);









    return 0;
}
